# Databricks Apps install list for EDIM DDE API.
#
# 1) Run:  ../scripts/build_vendor_wheels.sh
# 2) Prefer:  -r requirements.vendor.txt   (generated with exact wheel names)
#    Or manually: pip install vendor/*.whl then the host lines below.
#
# Full guide: edim-dde-domain/docs/api/deploy-and-hosting.md

-r requirements.vendor.txt

# Host
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.0
# Note: markdown (engineer /guide) is Docker-local only — see deploy/docker/requirements.txt

# Domain runtime extras (SQL + Foundry)
databricks-sql-connector>=3.0
azure-identity>=1.15
openai>=1.40
azure-keyvault-secrets>=4.8

# Postgres StateStore / RecommendationStore / LangGraph checkpointer
psycopg[binary]>=3.1
psycopg-pool>=3.2
langgraph-checkpoint-postgres>=2.0
